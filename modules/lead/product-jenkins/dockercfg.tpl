{
  "auths": {
    "${artifactory_url}": {
      "email": "${email}",
      "auth": "${artifactory_auth}"
    },
    "${harbor_url}": {
      "email": "${email}",
      "auth": "${harbor_auth}"
    }
  }
}
